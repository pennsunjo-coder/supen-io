[functions.extract-pdf]
verify_jwt = false

[functions.fetch-url]
verify_jwt = false

[functions.search-web]
verify_jwt = false

[functions.generate-embedding]
verify_jwt = false

# Called by anonymous visitors (waitlist confirmation + contact form),
# so JWT must not be required.
[functions.send-email]
verify_jwt = false

# Stripe calls this server-to-server with no Supabase JWT (it uses its own
# signature verification), so JWT must not be required.
[functions.stripe-webhook]
verify_jwt = false
